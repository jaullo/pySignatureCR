language: python
#sudo: required
python:
  - "3.7"
#  - "pypy"
#  - "pypy3"
#matrix:
#  allow_failures:
#    - python: pypy
#    - python: pypy3
before_install:
    - python3 --version
    - pip3 install -U pip
    - pip3 install -U pytest
    - pip3 install codecov
install:
  - "pip3 install -e git+https://github.com/jaullo/pySignatureCR.git@1.0.0#egg=pysignaturecr"
  - pip3 install "setuptools<34"
  - pip3 install "xmlsig"
  - pip3 install "pytz"
  - pip3 install "lxml"
  - pip3 install "datetime"
  - pip3 install "xmlunittest",
  - pip3 install "pytest"
  - pip3 install "pytest-cov"
script:
  - pytest --cov=./
after_success:
  - codecov